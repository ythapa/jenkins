pipeline{

    agent {
        dockerfile true
    }

    stages {
        stage ('check'){
            step ('build') {
                echo "hi"
            }
        }
    }
    
    triggers {
        pollSCM ('* * * * *')
    }
}
